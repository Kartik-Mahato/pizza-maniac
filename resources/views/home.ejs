<!-- Hero section starts -->
<section class="hero py-16">
    <div class="container mx-auto flex items-center justify-between">
        <div class="w-1/2">
            <h6 class="text-lg"><em>Are you hungry?</em></h6>
            <h1 class="text-3xl md:text-6xl font-bold">Don't Wait!!!</h1>
            <button class="px-6 py-2 rounded-full text-white font-bold mt-4 btn-primary">Order Now</button>
        </div>
        <div class="w-1/2">
            <img src="/img/pizza69.png" alt="pizza69">
        </div>
    </div>
</section>
<!-- Hero esection nds -->

<!-- menu starts -->
<section class="menu container mx-auto mb-4">
    <h1 class="text-xl font-bold mb-8">All Pizzas:</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 col-gap-12 gap-y-6">
        <% pizzas.forEach(function(pizza) { %>
            <div>
                <img src="/img/piz1.jpg" class="h-40 mb-4 mx-auto" alt="pizza">
                <div class="text-center">
                    <h2 class="mb-4 text-lg">
                        <%= pizza.name %>
                    </h2>
                    <span class="py-1 px-4 rounded-full uppercase text-xs size">
                        <%= pizza.size %>
                    </span>
                    <div class="flex items-center justify-around mt-6">
                        <span class="font-bold">₹<%= pizza.price %></span>
                        <button data-pizza="<%= JSON.stringify(pizza) %>"
                            class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
                            <span>+</span>
                            <span class="ml-4">Add</span>
                        </button>
                    </div>
                </div>
            </div>
            <% }) %>
    </div>
</section>
<!-- menu ends -->